apiVersion: v1
kind: Service
metadata:
  name: lunch-backend
  namespace: lunch
spec:
  type: NodePort
  selector: { app: lunch-backend }
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
